### Redux is a single independent "state Manangement Library" that works with the Any JS App . [i.e. Also solves the Prop-drilling problem but with the easy steps and it is easy to understand ] : je context API na  bhi samjah aya taah bhi koi nhi because redux sikhlo ohne bhi ehi kaam krna oh bhi asaan tarike naal 
- Like as the talwind CSS that works where is the HTML present . Similarly where is JS present Redux (Redux-toolkit[RTK]) works . Example - IT works in JS , React , Angular , Vue etc .
- It just integrates with React easily and do its state management task 
- It is totally independent library
- We have to install it externally and it is not the part of React 
- Redux (independent library) , React-redux(Redux for React ) , Redux-toolkit (advanced version of Redux )

### How to use Redux - toolkit 
1. Install both the commands from the redux official website 
2. First we have to create a store(common/global data where will be stored) . The best practice to create it is creating a "App" folder in "src" .
Then , creating a "Store.js" file in it .
3. Importing the "configureStore" from the reduxjs/toolkit in this "store.js" File . 
4. Create the Reducers ( functions ) . 